# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies and build output
node_modules
dist
dist-ssr
tsconfig.tsbuildinfo
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.working

# Claude Code: the project instructions are versioned, the local config is not.
# The glob must be `.claude/*`, not `.claude/`, or the negations below cannot
# reach inside an ignored directory.
.claude/*
!.claude/CLAUDE.md
!.claude/rules/

# Test coverage
coverage
